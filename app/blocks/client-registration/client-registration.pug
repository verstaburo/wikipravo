include ../icon/icons
include ../button/button

mixin client-registration(data)
  +b.client-registration&attributes(attributes)
    +e.img
      +icon(data.image)
    +e.H2.title= data.title
    +button-arrow("right")._white.__button Регистрация
