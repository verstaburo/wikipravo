include ../input/input
include ../icon/icons

mixin input-password(placeholder, id, pattern)
  +b.input-password&attributes(attributes)
    +input(type="password", id=id, placeholder=placeholder pattern=pattern required).__password
    +icon("eye").__button
