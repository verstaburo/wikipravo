extends ../../layouts/default

block head
  - var pageTitle = 'Wiki Pravo — Счет на сайте'

block content
  +b.account
    +page-title("Счет на сайте").__title
    +account-info(true).__info
    +account-history(false).__history

block page-left
  +sidebar

block page-top
  +header(true)

block page-bottom
  +footer

block page-popups
  +popups
    +popup-login
    +popup-account-replenish
