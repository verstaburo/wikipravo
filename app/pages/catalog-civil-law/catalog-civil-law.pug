extends ../../layouts/default

block head
  - var pageTitle = 'Wiki Pravo — Каталог юристов'

block content
  +b.catalog
    +catalog-navigation(getData('catalog-civil-navigation'), false)._lightblue
    +catalog-search(false)
      +catalog-cards-request(false).__catalog-cards
    +page-navigation(getData('page-navigation'))

block page-left
  +sidebar

block page-top
  +header(false, true)

block page-bottom
  +footer
