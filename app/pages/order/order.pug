extends ../../layouts/default

block head
  - var pageTitle = 'Wiki Pravo — Заказать'

block content
  +b.order
    +order-form
      +order-candidate
      +order-document
      +order-settings
      +order-registration

block page-left
  +sidebar

block page-top
  +header(true)

block page-bottom
  +footer

block page-popups
  +popups
    +popup-login
    +popup-order-success
