extends ../../layouts/default

block head
  - var pageTitle = 'Wiki Pravo — Просмотр заявки специалист'

block content
  +b.request
    +request-head(getData('request-jurist-header'), true)
    +request-card(true)

block page-left
  +sidebar

block page-top
  +header(true)

block page-bottom
  +footer

block page-popups
  +popups
    +popup-login
