extends ../../layouts/default

block head
  - var pageTitle = 'Wiki Pravo — Просмотр заявки клиент'

block content
  +b.request_client
    +request-head(getData('request-client-header'), false, "back")._client
    +request-content(false)

block page-left
  +sidebar

block page-top
  +header

block page-bottom
  +footer

block page-popups
  +popups
    +popup-login
